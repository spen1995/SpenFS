# SpenFS (prototype)

Minimal prototype for SpenFS. This project demonstrates dataset initialization with a 512-byte salt and Argon2id key derivation parameters. It's an early scaffold for the on-disk header and KDF integration.

Quick run:

```bash
cargo run -- init ./data
```
